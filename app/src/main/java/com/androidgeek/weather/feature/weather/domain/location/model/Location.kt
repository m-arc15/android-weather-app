package com.androidgeek.weather.feature.weather.domain.location.model

/**
 * A data class representing a geographic location. A location consists of a latitude and longitude.
 */
data class Location(
        val lat: Double,
        val long: Double
)
